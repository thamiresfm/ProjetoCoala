{
  "version": "1.0.0",
  "description": "projeto Coala",
  "scripts": {
    "test": "npx cypress run",
    "cy:open": "npx cypress open",
    "cy:allure": "npx cypress run --env allure=true"
  },
  "devDependencies": {
    "allure-commandline": "^2.23.0",
    "cypress": "^12.16.0",
    "cypress-junit-reporter": "^1.3.1"
  }
}
